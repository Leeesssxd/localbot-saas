# frontend/.env.example
# Copy to .env.local for local dev

# Points to backend API.
# In local dev you can leave this empty: Vite proxies /api to http://localhost:3000.
# If your backend runs somewhere else, set the full API base URL here.
# In production: set to your Railway/Render backend URL.
VITE_API_URL=
