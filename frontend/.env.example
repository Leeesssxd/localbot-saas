# frontend/.env.example
# Copy to .env.local for local dev

# Points to backend API
# In dev: the Vite proxy rewrites /api → http://localhost:3000, so leave this empty
# In production: set to your Railway/Render backend URL
VITE_API_URL=
